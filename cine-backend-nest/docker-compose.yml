services:
  app:
    build:
      context: .
      target: development
    container_name: ${APP_CONTAINER_NAME:-cine-backend-nest}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/dist
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3000
      - POSTGRES_HOST=db
      - DATABASE_URL=postgres://${POSTGRES_USER:-nodejs}:${POSTGRES_PASSWORD:-123456}@db:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-postgres}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cine-network

  db:
    image: postgres:15-alpine
    container_name: ${DB_CONTAINER_NAME:-cine-postgres-db}
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-nodejs}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-123456}
      - POSTGRES_DB=${POSTGRES_DB:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nodejs} -d ${POSTGRES_DB:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - cine-network

volumes:
  postgres_data:
    name: cine_postgres_data

networks:
  cine-network:
    name: cine_backend_network
    driver: bridge
